spring:
  jpa:
    properties:
      hibernate:
        format_sql: true
    show-sql: false

logging:
  level:
    com.quickserve: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.flywaydb: INFO

quickserve:
  jwt:
    secret: dev-secret-key-minimum-256-bits-long-for-hmac-sha256-algorithm-use
